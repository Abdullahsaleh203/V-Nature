extends base

block content
  main.auth-main
    .auth-container
      .auth-form-container
        .auth-header
          h2.auth-title Create your account
          p.auth-subtitle Join us to explore the world's most beautiful destinations

        form.auth-form.form--signup
          .form__group
            label.form__label(for='name') Full Name
            .input-container
              i.input-icon 👤
              input#name.form__input(type='text', placeholder='John Doe', required)
          
          .form__group
            label.form__label(for='email') Email address
            .input-container
              i.input-icon ✉
              input#email.form__input(type='email', placeholder='you@example.com', required)
          
          .form__group
            label.form__label(for='password') Password
            .input-container
              i.input-icon 🔒
              input#password.form__input(type='password', placeholder='••••••••', required, minlength='8')
          
          .form__group
            label.form__label(for='passwordConfirm') Confirm Password
            .input-container
              i.input-icon 🔒
              input#passwordConfirm.form__input(type='password', placeholder='••••••••', required, minlength='8')
          
          .form__terms
            input#terms.terms-checkbox(type='checkbox', required)
            label.terms-label(for='terms') 
              | I agree to the 
              a.terms-link(href='/terms') Terms of Service
              |  and 
              a.terms-link(href='/privacy') Privacy Policy
          
          .form__group
            button.btn.btn--auth Sign up
          
          //- .auth-divider
          //-   span or
          
          //- .social-login
          //-   button.btn.btn--social.btn--google
          //-     span.social-icon G
          //-     span Continue with Google
            
          //-   button.btn.btn--social.btn--facebook
          //-     span.social-icon f
          //-     span Continue with Facebook
          
          .auth-footer
            p Already have an account? 
              a.auth-link(href='/login') Sign in

      .auth-image
        img(src='/img/tours/tour-2-cover.jpg', alt='Beautiful Tour Image')
        .auth-image-overlay
        .auth-image-content
          h3.auth-image-title Start Your Journey Today
          p.auth-image-text Create an account to book amazing tours and receive special offers
          .auth-features
            .auth-feature
              i.auth-feature-icon ✓
              span Exclusive tour discounts
            .auth-feature
              i.auth-feature-icon ✓
              span Save your favorite tours
            .auth-feature
              i.auth-feature-icon ✓
              span Fast checkout experience

  style.
    .auth-main {
      min-height: calc(100vh - 8rem);
      display: flex;
      align-items: center;
      justify-content: center;
      background-color: var(--color-grey-light-1);
      padding: 3rem 1rem;
    }
    
    .auth-container {
      display: flex;
      width: 100%;
      max-width: 1100px;
      min-height: 660px;
      background-color: var(--color-white);
      border-radius: var(--border-radius-large);
      overflow: hidden;
      box-shadow: var(--shadow-medium);
    }
    
    .auth-form-container {
      flex: 1;
      padding: 4rem;
      display: flex;
      flex-direction: column;
      overflow-y: auto;
    }
    
    .auth-header {
      margin-bottom: 3rem;
      text-align: center;
    }
    
    .auth-title {
      font-size: 2.8rem;
      font-weight: 700;
      color: var(--color-primary);
      margin-bottom: 1rem;
    }
    
    .auth-subtitle {
      font-size: 1.5rem;
      color: var(--color-grey-dark-1);
    }
    
    .auth-form {
      width: 100%;
      max-width: 40rem;
      margin: 0 auto;
    }
    
    .input-container {
      position: relative;
    }
    
    .input-icon {
      position: absolute;
      top: 50%;
      left: 1.5rem;
      transform: translateY(-50%);
      color: var(--color-grey-dark-1);
      font-style: normal;
    }
    
    .form__input {
      width: 100%;
      padding: 1.2rem 1.5rem 1.2rem 4rem;
      border: 1px solid var(--color-grey-light-2);
      border-radius: var(--border-radius-small);
      font-family: inherit;
      font-size: 1.5rem;
      color: var(--color-grey-dark-2);
      transition: var(--transition-medium);
      margin-bottom: 1.5rem;
    }
    
    .form__input:focus {
      outline: none;
      border-color: var(--color-tertiary);
      box-shadow: 0 0 0 2px rgba(230, 126, 34, 0.1);
    }
    
    .form__label {
      display: block;
      font-size: 1.5rem;
      font-weight: 600;
      margin-bottom: 0.8rem;
      color: var(--color-primary);
    }
    
    .form__terms {
      display: flex;
      align-items: flex-start;
      gap: 1rem;
      margin: 1rem 0 2.5rem;
    }
    
    .terms-checkbox {
      width: 1.6rem;
      height: 1.6rem;
      margin-top: 0.3rem;
      accent-color: var(--color-tertiary);
    }
    
    .terms-label {
      font-size: 1.4rem;
      line-height: 1.6;
      color: var(--color-grey-dark-1);
    }
    
    .terms-link {
      color: var(--color-tertiary);
      text-decoration: none;
      transition: var(--transition-fast);
    }
    
    .terms-link:hover {
      color: var(--color-tertiary-dark);
      text-decoration: underline;
    }
    
    .btn--auth {
      width: 100%;
      background-color: var(--color-tertiary);
      color: var(--color-white);
      padding: 1.4rem;
      font-size: 1.6rem;
      font-weight: 600;
      border: none;
      border-radius: var(--border-radius-small);
      cursor: pointer;
      transition: var(--transition-medium);
    }
    
    .btn--auth:hover {
      background-color: var(--color-tertiary-dark);
      transform: translateY(-3px);
    }
    
    .auth-divider {
      position: relative;
      text-align: center;
      margin: 2.5rem 0;
    }
    
    .auth-divider::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 0;
      right: 0;
      height: 1px;
      background-color: var(--color-grey-light-2);
      z-index: 1;
    }
    
    .auth-divider span {
      position: relative;
      background-color: var(--color-white);
      padding: 0 1.5rem;
      font-size: 1.4rem;
      color: var(--color-grey-dark-1);
      z-index: 2;
    }
    
    .social-login {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }
    
    .btn--social {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 1rem;
      width: 100%;
      padding: 1.2rem;
      border: 1px solid var(--color-grey-light-2);
      border-radius: var(--border-radius-small);
      background-color: transparent;
      font-size: 1.5rem;
      font-weight: 500;
      color: var(--color-grey-dark-2);
      cursor: pointer;
      transition: var(--transition-medium);
    }
    
    .btn--social:hover {
      background-color: var(--color-grey-light-1);
    }
    
    .social-icon {
      font-weight: 700;
      font-size: 1.8rem;
    }
    
    .btn--google .social-icon {
      color: #4285F4;
    }
    
    .btn--facebook .social-icon {
      color: #3b5998;
    }
    
    .auth-footer {
      margin-top: 3rem;
      text-align: center;
      font-size: 1.5rem;
      color: var(--color-grey-dark-1);
    }
    
    .auth-link {
      color: var(--color-tertiary);
      text-decoration: none;
      font-weight: 600;
      transition: var(--transition-fast);
    }
    
    .auth-link:hover {
      color: var(--color-tertiary-dark);
      text-decoration: underline;
    }
    
    .auth-image {
      display: none;
      position: relative;
      flex: 1;
    }
    
    .auth-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    
    .auth-image-overlay {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(to bottom right, rgba(44, 62, 80, 0.7), rgba(230, 126, 34, 0.8));
    }
    
    .auth-image-content {
      position: absolute;
      bottom: 4rem;
      left: 0;
      right: 0;
      padding: 0 3rem;
      color: var(--color-white);
      text-align: center;
    }
    
    .auth-image-title {
      font-size: 2.8rem;
      font-weight: 700;
      margin-bottom: 1.5rem;
    }
    
    .auth-image-text {
      font-size: 1.6rem;
      line-height: 1.6;
      margin-bottom: 2.5rem;
    }
    
    .auth-features {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }
    
    .auth-feature {
      display: flex;
      align-items: center;
      gap: 1rem;
      font-size: 1.5rem;
    }
    
    .auth-feature-icon {
      font-size: 1.8rem;
      font-style: normal;
    }
    
    @media only screen and (min-width: 768px) {
      .auth-image {
        display: block;
      }
      
      .auth-form-container {
        padding: 5rem;
      }
    }
    
    @media only screen and (max-width: 767px) {
      .auth-container {
        max-width: 500px;
      }
      
      .auth-form-container {
        padding: 3rem 2rem;
      }
    }
